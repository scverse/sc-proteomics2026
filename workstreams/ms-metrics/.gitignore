# python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# environments
.venv/

# packaging
build/
dist/
*.egg-info/

# tooling caches
.pytest_cache/
.ruff_cache/

# data
data/

# notebooks are tracked as jupytext py:percent scripts, the .ipynb is a local build artifact
*.ipynb
# ...except this one, which is a hand-written narrative doc, not a build artifact
!analysis_steps.ipynb

# rendered notebooks are build artifacts, like the .ipynb above
analysis.html
